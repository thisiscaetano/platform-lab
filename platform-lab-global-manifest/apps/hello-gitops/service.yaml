apiVersion: v1
kind: Service
metadata:
  name: hello-gitops
  namespace: hello-gitops
  labels:
    app: hello-gitops
spec:
  selector:
    app: hello-gitops
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
